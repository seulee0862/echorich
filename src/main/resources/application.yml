spring:
  profiles:
    group:
      "local": "local, naver"
      "default": "local, naver"
      "product" : "product, naver"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        default_batch_fetch_size: 100